"""
from typing import List

def greet_all(names: List[str]) -> None:
    for name in names:
        print(name)


greet_all(["Guilherme", "Giovanna"])
"""


def greet_all(names: list[str]) -> None:
    for name in names:
        print(name)


greet_all(["Guilherme", "Giovanna"])
